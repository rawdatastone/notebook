Just a simple sqlite3 Database to mess about with SQL statements for practice.
These scripts are not stand alone, rather sequential. This is to isolate action per script.

Parts:
    data in a csv file
    python script to create a database and a table
    python script to insert data from a csv to database table 
    python script to query a db and print result from DataFrame
    python script to export query results into a csv file
    output csv file ( may be missing as generated by script )

SSMS
    
    Add-on to use SSMS with sqlite:
    https://marketplace.visualstudio.com/items?itemName=ErikEJ.SQLServerCompactSQLiteToolboxforSSMS#overview
